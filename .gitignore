CLAUDE.md
.claude/
screenshot/
*.zip
.env*
node_modules/
.DS_Store
Thumbs.db

# Per-prototype Nano Banana staging — original AI exports stay here, never committed
_public/

# Agent-internal artifacts (lead research, prospect lists, search prompts, decisions, image prompts, build log)
# Family-wide rule (locked 2026-05-05): the entire _docs/ folder is gitignored. Build logs stay between
# Bithun and the agent — Google Drive Desktop sync provides cross-machine durability, GitHub is not the
# durability mechanism. If the repo flips to private later, Bithun may opt-in to push _docs/ at that point.
_docs/

# Test/preview artifacts
.playwright-mcp/
*.png

# IDE config
.vscode/
.idea/
